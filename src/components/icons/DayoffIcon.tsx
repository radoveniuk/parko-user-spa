
import React from 'react';
import { IconBaseProps } from 'react-icons';
import { MdWeekend } from 'react-icons/md';

const DayoffIcon = ({ ...rest }: IconBaseProps) => <MdWeekend {...rest} />;

export default DayoffIcon;
